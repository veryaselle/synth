{
  "experiment": "independent split-realization robustness check",
  "role": "supplementary robustness analysis; primary frozen benchmark remains unchanged",
  "realization_seed": 2026,
  "n_splits_per_dataset": 5,
  "test_size": 0.2,
  "partition_policy": {
    "pima": "five stratified train_test_split calls with seeds 2026, 2027, 2028, 2029, 2030",
    "cleveland": "StratifiedShuffleSplit(n_splits=5, test_size=0.2, random_state=2026)",
    "ckd": "StratifiedShuffleSplit(n_splits=5, test_size=0.2, random_state=2026)"
  },
  "held_constant_from_primary": {
    "generator_seed_schedule": "42 + split_id",
    "evaluator_seed": 42,
    "release_size": "1x",
    "generator_hyperparameters": "frozen primary configurations",
    "classifiers": ["LR", "MLP", "Random Forest", "XGBoost"],
    "utility_metrics": ["AUROC", "F1@0.5", "Brier"],
    "fidelity_metrics": ["PCD", "normalized Wasserstein", "Jensen-Shannon"],
    "privacy_audits": ["DCR descriptive", "MIA AUROC", "AIA risk"]
  },
  "primary_question": "Are the qualitative generator rankings and dimension-level selections robust to an independently shuffled train/test partition realization?",
  "interpretation_boundary": "Retraining stochastic generators can contribute residual run variation. This is not a complete repeated-generator-seed experiment."
}
