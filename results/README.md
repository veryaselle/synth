# Results directory

Tracked files are validated summaries, release audits and frozen artifacts used by the thesis. Checkpoints, chunks and prediction folders are excluded.

The Diabetes 130-US `evaluation_snapshot` CSVs were transcribed from the completed console output and preserve the exact headline values. For paired uncertainty analysis, retain or regenerate the complete local `utility_runs.csv` and prediction files.
